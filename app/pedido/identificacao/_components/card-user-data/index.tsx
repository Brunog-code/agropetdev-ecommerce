"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export const CardUserData = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-lg">Dados do usuario</CardTitle>
      </CardHeader>

      <CardContent>
      
      </CardContent>
    </Card>
  );
};
