import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["wcpvdrfhvnarjagqwhho.supabase.co"], // adicione todos os domínios externos que usar
  },
};

export default nextConfig;
